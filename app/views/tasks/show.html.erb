<div class="d-flex justify-content-between align-items-end">
  <h1><%= @task.title %></h1>
  <%= link_to "✏️ Edit this task", edit_path(@task) %>
</div>

<div class="border rounded my-2 px-2">
  <p><%= @task.details %></p>
</div>

<%= link_to "⟲ Back to list", new_path %>
