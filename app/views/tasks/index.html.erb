<div class="container my-3">
  <h1>My Tasks</h1>
</div>

<div class="container list-decoration-none">
  <ul id="tasks">
    <% @tasks.each do |task| %>
     <% if task.completed %>
        <input type="checkbox" checked disabled>
      <% else %>
        <input type="checkbox" disabled>
      <% end %>
      <%= link_to task.title, task_path(task) %>
      ·
      <%= link_to "🗑", task_path(task.id), method: :delete, data: {confirm: "Confirm to delete?"} %>
      <br>
    <% end %>
</ul>
</div>

<div class="container">
  <%= link_to "➕ Add a new task", new_task_path %>
</div>

